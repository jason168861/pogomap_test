// ---------------------------------------------------------------------------
// 設定檔。要改設定只動這個檔案，其他都不用動。
// ---------------------------------------------------------------------------
window.APP_CONFIG = {

  // 資料來源：
  //   'direct' = 瀏覽器直接跟 Niantic 要（不用任何後端，GitHub Pages 直接就能動）
  //   網址字串 = 跟你的 Cloudflare Worker 要快照，例如：
  //              'https://taoyuan-map.你的帳號.workers.dev/data'
  source: 'direct',

  // 一般資料的更新間隔（秒）。0 = 不自動更新。
  // 這一趟「不帶 token」，抓道館 / 補給站 / 團體戰 / 活動。
  // 每次 5 個請求約 3.3 MB，120 秒約等於 100 MB/小時。
  // 網址加 ?refresh=300 可以臨時改成 5 分鐘（手機吃行動網路時好用）。
  refreshSec: 120,

  // 能量點（極巨化）的更新間隔（秒）。這一趟「會帶 token」，也就是會暴露帳號，
  // 所以間隔拉長、而且只在「能量點圖層有打開 + 有設定 token」時才會跑。
  powerRefreshSec: 300,

  // 每幾輪做一次「全區完整掃描」來發現新的能量點位置。
  // 其他輪只查已知有能量點的格子（通常 1 個請求就夠），把帶 token 的請求量壓到最低。
  // 12 輪 × 5 分鐘 = 每小時完整掃一次。
  powerFullSweepEvery: 12,

  // Google Maps API key —— ★ 這裡永遠留空，不要填。
  // 正式部署時由 .github/workflows/deploy.yml 從 GitHub Secret（MAPS_API_KEY）
  // 在 build 階段「附加一行」覆寫它，所以 key 不會進 repo。
  // 本機開發時是空的，地點搜尋會自動退回 OpenStreetMap。
  mapsApiKey: '',

  // 預設底圖：'voyager'(亮，道路清楚) / 'light'(極簡亮) / 'dark'(暗) / 'satellite'(衛星)
  basemap: 'voyager',

  // 地圖初始位置（桃園車站附近）與縮放
  center: [25.0100, 121.2650],
  zoom: 13,

  // 補給站六千多個，縮太遠不畫，避免卡頓
  stopMinZoom: 14,
  // 放到這個倍率以上，補給站改用圖示（而不是小圓點）
  stopIconZoom: 17,

  // ---- 效能（手機主要吃這幾項）----

  // 補給站圖層預設關掉。補給站是數量最多的標記，手機上最主要的卡頓來源。
  //   'mobile' = 只有窄螢幕預設關 / true = 一律預設關 / false = 一律預設開
  stopDefaultOff: 'mobile',

  // 縮到這個倍率以下就「什麼標記都不畫」，只留底圖。
  // 那個距離下標記本來就擠成一團看不出東西，畫了只是白費效能。
  markerMinZoom: 14,

  // 畫面上的團體戰超過這個數量，就把道館外圈那圈脈動光暈停成靜止的圓圈。
  // 那圈是 CSS 無限迴圈動畫，每一個都會被瀏覽器提升成獨立的合成層，
  // 幾十個以上就會持續吃 GPU —— 就算你完全沒在操作也一樣在跑。
  // 停掉動畫後圈圈還在（變成靜止的），一樣看得出哪些道館有團體戰。
  maxPulseRings: 30,

  // 縮到這個倍率以下，「沒有團體戰的」道館改用小圓點。
  // 完整圖示要載 PNG 又套了 drop-shadow 濾鏡，每個標記都是一次獨立合成，
  // 幾百個一起在手機上拖曳/縮放就是主要的卡頓來源。圓點只是一個純色的 <i>。
  // 有團體戰的道館一律保留完整圖示 —— 那才是要找的東西，不能因為縮放就看不到。
  gymIconZoom: 13,
  gymIconZoomMobile: 15,

  // 只畫視野內的標記，但視野外會多畫一圈當緩衝，小幅度拖曳才不會看到空白。
  // 數字是視野邊長的比例（0.25 = 四周各多畫 25%，等於畫了 2.25 個畫面的量）。
  // 重繪是在拖曳「結束」時才發生，所以這個值調太小的話，
  // 拖曳過程中邊緣會先空白一下、放開才補上。手機是效能和這個空窗的取捨。
  viewPad: 0.25,
  viewPadMobile: 0.12,   // 約 1.25 個畫面，比桌機少畫四成

  // S2 網格預設 level（14 = 道館網格，17 = 補給站網格）
  gridLevel: 17,

  // 小人身上的兩個範圍圈（公尺）
  personRadius: [40, 80],

  // ---- 以下通常不用改 ----
  api: 'https://niantic-social-api.nianticlabs.com/graphql',
  realityChannelId: 'da83476a-c4da-4312-a610-a4f2fc2c37f0',
  s2CellLevel: 14,   // 實測 12/13 會漏 70~95% 的物件，一定要用 14
  batchSize: 200,    // 一個請求帶幾個 cell（實測 400 也安全）
  parallel: 3        // 同時最多幾個請求在飛
};
